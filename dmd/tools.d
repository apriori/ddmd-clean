// Some code which helps. 5yy line 2
import std.stdio;
static int zachCount = 0;
++zachCount;
writefln("codeHit hit #%s", zachCount);
//writefln("newParam #%s: at: %s ai: %s ae: %s",
//     zachCount, at.toChars(), ai.toChars(), ae.toChars() );
